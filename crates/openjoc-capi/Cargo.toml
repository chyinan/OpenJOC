[package]
name = "openjoc-capi"
version.workspace = true
description = "Versioned C ABI for the OpenJOC headless streaming session"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lib]
crate-type = ["rlib", "staticlib", "cdylib"]

[dependencies]
openjoc-api = { version = "0.10.0", path = "../openjoc-api" }
openjoc-ffmpeg = { version = "0.10.0", path = "../openjoc-ffmpeg" }

[lints]
workspace = true
