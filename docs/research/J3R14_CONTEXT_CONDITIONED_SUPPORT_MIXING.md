# J3R14 — Context-conditioned ReconstructionBasis support mixing

J3R14 is a bounded numerical analysis of the existing, exact-N3-calibrated
corpus. It does not create media, launch Logic, reopen C3, or change decoder
semantics.

The predeclared support floor was `9.54129888914455e-7`: four times the
frozen J2R7 N0/N1 ReconstructionBasis envelope, with the exact J3R13 producer
null at zero. The floor was frozen before evaluating target support.

For C1 (static Front Left versus the dual-object pre-window), the static
vector has support `{row_000}`, while the dual vector has support
`{row_000,...,row_008}`. For C2 (static Front Right versus the dual-object
post-window), the same support transition is observed. These are coordinate
labels only; rows are not authored objects.

The global complex-gauge residuals are `0.0031748106398913224` (C1) and
`0.9999992742845898` (C2). A support-preserving diagonal model cannot create
the observed dual-only coordinates. A shared diagonal model and a bounded
common-additive model both fail cross-prediction. No free row-permutation
search was performed or admitted. Neither dual vector matches a frozen
single-position static signature within the exact producer envelope, and both
project outside the frozen static-atlas span.

The narrow result is:

`J3R14_CONTEXT_DEPENDENCE_REQUIRES_SUPPORT_CREATING_CROSS_COORDINATE_MIXING`

This is a ReconstructionBasis-coordinate statement within the tested C1/C2
corpus. It does not identify the causal hidden variable, Dolby semantics,
slot causality, authored-object identity, authored-object PCM, renderer
behavior, or a final JOC synthesis transform. `SemanticBindingState` remains
`Unresolved`; `RcLfe` remains separate; warp raw 3 remains ETSI
`ReservedWarpMode`.

The single minimum future experiment, if pursued, is a companion on/off
intervention with the target position, source, and producer settings fixed.
It is recommended only to separate a population/context effect from a
position-signature reindexing hypothesis; it was not executed in J3R14.
