# Custom speaker layouts

The canonical custom speaker layout guide is maintained in the [OpenJOC documentation site](site/using/custom-speaker-layouts.md).

The source contract supports custom geometry up to 64 output channels.

This repository path is retained as a short compatibility pointer for existing links.
