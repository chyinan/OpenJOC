# Archived release documents

These directories contain immutable, version-specific records. Historical
version numbers and limitations are intentional and must not be interpreted as
the current product contract.
